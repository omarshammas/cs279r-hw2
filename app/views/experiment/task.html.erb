<%= render 'collect_data' %>

<% if !@ribbon %>
	<div id="commandmap">
		<%= render 'commandmaps'%>
	</div>
<% end %>

<%= hidden_field_tag "tab_index", @tab_index, {:id => "tab_index"} %>

<div id="ribbon">

	<% if @ribbon %>	
		<span class="ribbon-window-title">Ribbons</span>
	<% else %>
		<span class="ribbon-window-title">Command Maps</span>
	<% end %>

<%= 
	if(@ribbon)
		render 'ribbon'
	end
%>

</div>
<div class="page">

	<div id="target_icon">
	</div>
	<br />
	<% if @ribbon %>
		 
	<% else %>
		Remember you have to hold the CTRL key on your keyboard down to activate the menu.
	<% end %>

	<br />
	<br />

	<% if flash[:notice] %>
	    <div id="notice" class="alert alert-error"><%= flash[:notice] %></div>
	<% end %>


	<br />
	<br />
</div>

